# Database Configuration Template
# Copy this file to 'application.properties' and fill in your actual values

# Database username (default for Supabase: postgres)
username=postgres

# Database password (use your Supabase project password)
password=YOUR_SUPABASE_PASSWORD

# JDBC driver class name
driverName=org.postgresql.Driver

# Database connection string
# Format: jdbc:postgresql://HOST:PORT/DATABASE?sslmode=require
# Replace YOUR_SUPABASE_HOST with your actual Supabase host from dashboard
# Example: db.xxxxxxxxxxxxx.supabase.co
connectionString=jdbc:postgresql://YOUR_SUPABASE_HOST:5432/postgres?sslmode=require

# ===========================================
# How to get your Supabase connection details:
# ===========================================
# 1. Go to: https://supabase.com/dashboard
# 2. Select your project
# 3. Click Settings (gear icon) → Database
# 4. Scroll to "Connection string" section
# 5. Select "JDBC" tab
# 6. Copy the connection details
#
# Your connection string will look like:
# jdbc:postgresql://db.abcdefghijkl.supabase.co:5432/postgres?user=postgres&password=[YOUR-PASSWORD]
#
# Extract:
# - Host: db.abcdefghijkl.supabase.co
# - Port: 5432 (default)
# - Database: postgres (default)
# - Password: The password you set when creating the project
